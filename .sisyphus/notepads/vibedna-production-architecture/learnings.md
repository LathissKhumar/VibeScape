- Observed repo uses strict runtime env checks (supabase.ts) — follow same pattern but keep Redis optional.
- Existing cache is Supabase table-based (spotify_cache); cache keys are human-readable strings with userId included.
- Chose Upstash Redis client (`@upstash/redis`) and implemented lazy init to avoid throwing when env missing.
- Implemented typed helpers: get, set, del, exists, keys. All degrade gracefully when Redis not configured or errors occur.
- For Postgres/Drizzle setup: added minimal, lazy-initialized db client at src/lib/db.ts that exports null when DATABASE_URL is absent to avoid breaking builds. Added db/README.md and migrations folder.
- For telemetry: added optional, build-safe Sentry initializer (src/lib/sentry.ts) and OpenTelemetry scaffolding (src/lib/otel.ts). Both use runtime/dynamic requires and only activate when corresponding env vars are set. A central importable telemetry initializer lives at src/middleware/telemetry.ts.
- For async workers: added QStash scaffold (src/lib/qstash.ts) and an example QStash route at src/app/api/qstash/route.ts that verifies request signature if the SDK is installed. Everything is opt-in via env vars.
 - Cache layer: added coreCache abstraction at src/lib/cache/index.ts implementing hot (Redis) -> warm (Postgres) -> cold (placeholder). Uses existing redis client and db scaffolding and degrades gracefully when services are absent.
- Added unit tests for cache layer: src/lib/cache/__tests__/index.test.ts verifying hot hit, warm fallback, and del behavior using Vitest and module mocks.
- Created initial DB migration SQL at db/migrations/0001_create_core_schema.sql containing minimal tables: users, tracks, events, snapshots. This is plain SQL so it remains build-safe until Drizzle is installed.
- Testing: Vitest is configured (vitest.config.ts) and existing unit tests run. Added testing notes at .sisyphus/notepads/vibedna-production-architecture/testing.md with recommendations for RTL and Playwright scaffolding.
- API Gateway: added a minimal Spotify proxy route at src/app/api/spotify/proxy/route.ts. It uses rateLimit and coreCache to apply per-token rate limiting and caching. The route expects a `path` query param and an Authorization: Bearer <token> header. It runs in Next "edge" runtime.
- Events system: added typed event map and minimal emitter at src/lib/events.ts. It supports local subscribers and persists events to the `events` table when DB is available. Uses raw SQL to avoid tight Drizzle coupling for now.
- Async workers: added minimal worker handlers:
  - src/workers/personality/handler.ts
  - src/workers/analytics/handler.ts
  - src/workers/embedding/handler.ts
  and an enqueueing scaffold route at src/app/api/workers/route.ts that publishes to QStash when enabled. All scaffolds are opt-in via QSTASH_ENABLED env var.
- Delta sync: added a minimal incremental sync engine at src/lib/sync/spotifySync.ts. It fetches top tracks/artists, compares to last snapshot, emits events for changes, and persists a new snapshot. Disabled unless ENABLE_DELTA_SYNC=true.
- Data layer refactor: rewired Spotify cache to prefer coreCache (Redis hot / Postgres warm) and fall back to Supabase for stale reads. Updated src/lib/spotify.ts to use coreCache first and write-through to coreCache and Supabase on fresh fetch.
- Observability: added observability helpers at src/lib/observability.ts for recording cache hits/misses and a placeholder cacheHitRatio function that integrates with Sentry when available.
- AI pipeline: added helpers at src/lib/aiPipeline.ts to compress structured audio/artist metrics into a compact delimited prompt and compute a deterministic fingerprint. This enables skipping AI calls when the fingerprint doesn't change and keeps prompts short for cheaper LLM usage.
 - Analytics precompute: added minimal scaffolding at src/lib/analytics.ts which exposes refreshMaterializedViews() and a refreshNightly() worker handler; both are no-ops when DATABASE_URL is absent and use raw SQL to refresh materialized views when available.
 - Embeddings: added minimal pgvector-aligned scaffolding at src/lib/embeddings.ts that computes deterministic pseudo-embeddings from genres/topArtists and provides upsertEmbedding() to persist vectors to an `embeddings` table (raw SQL). This is intentionally lightweight and assumes pgvector/embeddings table exist when DATABASE_URL is provided.
 - Recommendation engine: added src/lib/recommend/index.ts with cosineSimilarity(), computeVibeCompatibility(), findMusicTwins(), and getPersonalizedRecommendations(). All functions use deterministic pseudo-embeddings from src/lib/embeddings.ts — no external AI or DB required. Built-in graceful degradation for empty/missing data (returns 0-score or fallback recommendations instead of throwing).
 - API routes: added /api/recommend/vibe-compatibility (POST, takes profileA+profileB) and /api/recommend/personalized (POST, takes profile+candidates). Both apply rate limiting (30 req/min) and hot cache (5min TTL) via src/lib/rateLimiter.ts and src/lib/cache/index.ts.
 - Dashboard UI: added src/components/dashboard/MusicTwinCard.tsx — "use client" component with loading skeleton, empty state, and filled state showing compatibility score bar, label, and shared genre badges. Follows existing card/skeleton patterns.
 - Tests: 22 tests covering cosine similarity edge cases, vibe compatibility scoring, music twin ranking, personalized recs with/without candidates, and empty-data fallbacks.
 - Build safety: quoted multi-word genre keys (hip hop, r&b) to avoid esbuild parse errors. Keep keys with special characters (&, spaces) quoted.
 - Cleanup: removed unused `userVec` variable from `findMusicTwins` that was introduced during initial implementation. The computed embedding was unused since `computeVibeCompatibility` recomputes it internally. (zero diff in behavior, 22 tests unchanged).

- Task 22 (Dashboard split): Extracted 8 components from DashboardClient.tsx (493→170 lines). Key pattern: components receive props for data, use useReducedMotion for animations, handle loading/empty/error states via LoadingSkeleton/EmptyState/ErrorState utilities.
- ListeningHeatmap exists as a full section component at src/components/ListeningHeatmap.tsx (352 lines) and was reused as-is via dynamic import in ListeningHeatmapSection.tsx.
- AudioFeaturesChart exists at src/components/AudioFeaturesChart.tsx (117 lines) and was reused as-is via AudioRadarSection.tsx.
- PersonalityCard exists at src/components/PersonalityCard.tsx (143 lines) and remains imported directly in DashboardClient.tsx for the Sonic DNA section.
- Three utility components (LoadingSkeleton, EmptyState, ErrorState) exist in src/components/ui/ and were used for loading/empty/error states in all extracted components.
- The AIInsightsSheet uses Sheet from @base-ui/react/dialog (not Radix) — the render-prop pattern on SheetTrigger works via base-ui's `render` prop API.
- DashboardContent.tsx still keeps Galaxy section inline (Task 23 owns enhancement), CTA section inline, and footer inline.
- ArtistCardsSection accesses artist.images and artist.external_urls.spotify via type widening ((artist as any)) since SpotifyArtist interface doesn't declare these fields. These fields exist in real Spotify API responses.
- Task 23 (Galaxy enhancement): Enhanced Galaxy.tsx with bloom postprocessing (via three-stdlib's UnrealBloomPass + EffectComposer, no new deps), particle trails per planet, genre constellation lines connecting same-genre artists, click-to-focus with animated camera transitions and info popup, orbital ring visualizations, genre cluster labels, parallax starfield (dual-layer Stars), and auto-rotate toggle. WebGL fallback uses existing useWebGL hook + WebGLFallback component. drei v10 doesn't export EffectComposer/Bloom directly — must import from three-stdlib and manage imperatively via useThree + useFrame.
- Task 23 (BloomEffect fix): Original BloomEffect had empty useEffect and created composer in useMemo — composer wasn't properly integrated with render loop. Fixed by moving composer creation to useEffect, calling gl.setAnimationLoop(null) to disable default renderer, and using useFrame at priority 1 to render through composer. This ensures bloom actually applies instead of double-rendering.
- Task 23 (WebGLFallback improvement): Replaced basic gray rectangle with animated starfield on dark background using 2D canvas. Added DPR scaling for crisp rendering. Component now accepts optional message prop.
- Task 23 (Test improvements): Fixed Galaxy.test.tsx import placement (was at bottom of file), added Canvas mock for when WebGL is supported, added third test verifying controls don't render in fallback mode. All 103 tests pass, build green.

- Task 27 (Feature flags): implemented Redis-backed feature flag helpers at src/lib/feature-flags/index.ts with local fallback and API route at src/app/api/feature-flags/route.ts. Added tests at src/lib/feature-flags/index.test.ts verifying default, set/get, and getAll behavior. Build and vitest pass locally. Feature flags reuse coreCache and respect absence of Redis/DB by falling back to in-memory store.

- Task 28 (CDN + image optimization): Added preconnect/dns-prefetch hints to src/app/layout.tsx for fonts and Spotify auth, and conservative Next Image remotePatterns/domains in next.config.ts (vercel.app, i.scdn.co, images.unsplash.com, cdn.sanity.io). No <img> or next/image usage was present to patch; build verified OK. See .sisyphus/notepads/vibedna-production-architecture/task-28-cdn-image-optimizations.md for details.
