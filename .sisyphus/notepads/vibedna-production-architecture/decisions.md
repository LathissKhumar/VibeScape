- Decision: Use minimal, non-breaking Drizzle client file (src/lib/db.ts) that dynamically requires dependencies only when DATABASE_URL is set. This keeps build passing even if new DB deps are not installed yet.
- Decision: Provide .env.example with DATABASE_URL placeholder and add db/README.md for future migration steps.
- Decision: Provide minimal telemetry scaffolding that is opt-in via env vars. Use dynamic requires for Sentry and OpenTelemetry to keep build non-breaking. Central initializer available at src/middleware/telemetry.ts for future import in server startup.
- Decision: Provide minimal QStash scaffolding that is opt-in via env vars and safe when SDK is absent. Example API route added at src/app/api/qstash/route.ts which returns 204 when QSTASH_ENABLED is false.
- Decision: Implement a minimal coreCache abstraction that prefers Redis (hot) and falls back to a Postgres table (warm). Cold tier left as a placeholder for future archival (S3) implementation. Use raw SQL through db.execute? as a loose coupling to avoid hard Drizzle dependency now.
- Decision: Add an optional Redis-backed sliding-window rate limiter (src/lib/rateLimiter.ts) that uses Redis sorted-sets and gracefully allows traffic when Redis is absent. Chosen to avoid blocking behavior in environments without Redis.
- Decision: Add an initial SQL migration (db/migrations/0001_create_core_schema.sql) to define users, tracks, events, snapshots as plain SQL so teams without Drizzle can still apply migrations via CLI or DB admin tools. Keep this minimal and production-safe.
- Decision: AI pipeline optimization should use compact, deterministic metrics fingerprints to avoid redundant LLM calls. Implemented src/lib/aiPipeline.ts which summarizes metrics, builds a compact prompt, and produces a sha256-derived fingerprint (truncated) for identity.
- Decision: Precomputed analytics will be implemented as materialized views refreshed by a nightly worker. Added a minimal, build-safe scaffold at src/lib/analytics.ts that refreshes materialized views using raw SQL when DB is present and emits a generic event after refreshing.
 - Decision: For initial vector support, use deterministic pseudo-embeddings for genres/profile (src/lib/embeddings.ts) so we can store and test vector storage without depending on an embeddings API. Persist to a lightweight embeddings table (pgvector) when DATABASE_URL is set; production should replace computeProfileEmbedding with a real embeddings API later.

- Decision (Task 22): Components remain prop-driven (receive personality, topArtists, audioFeatures as props) instead of wiring into getPrecomputedDashboard (Task 16) since that function doesn't exist yet. Refactoring to precomputed data later is a simple props→hooks change.
- Decision (Task 22): Use reducible motion semantics: each extracted section imports useReducedMotion() and conditionally disables framer-motion animate/initial when prefers-reduced-motion is active.
- Decision (Task 22): Galaxy/CTA/Footer sections remain inline in DashboardClient.tsx to avoid scope creep into Task 23 (Galaxy enhancement). These account for ~90 of the remaining 170 lines.
- Decision (Task 22): Avatar images for artist cards added via img tag with opacity/group-hover transitions. Spotify external link added as ExternalLink icon. Both use type widening since the SpotifyArtist interface doesn't declare these fields yet.
