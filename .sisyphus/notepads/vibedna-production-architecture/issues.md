- Issue: Drizzle and pg packages are not installed in this repo; static imports caused build to fail. Workaround: use dynamic require and export null when DATABASE_URL missing. Next step: install drizzle-orm, drizzle-kit, and pg in CI or when ready.
- Issue: Telemetry packages (@sentry/node, @opentelemetry/*) are not installed. Implemented dynamic-require scaffolding to avoid build failures. Next step: install desired telemetry SDKs in CI or when enabling telemetry.
- Issue: QStash package (@upstash/qstash) is not installed. Implemented dynamic-require scaffolding for QStash and an example route. Next step: install @upstash/qstash and set QSTASH_* env vars to enable.
 - Issue: Warm cache uses raw SQL against `spotify_cache` table and assumes db.execute exists. Drizzle pool may have different methods; this is an intentionally minimal, loosely-typed scaffold. Next step: replace raw SQL with Drizzle queries when Drizzle is installed/configured.

- Issue: next/dynamic with ssr:false does not resolve in jsdom test environments. Workaround: mock next/dynamic in vitest tests and test wrapper behavior rather than dynamically-loaded content. Or mock the dynamically imported module directly.
- Issue: SpotifyArtist interface in src/lib/spotify.ts does not include `images` or `external_urls` fields. ArtistCardsSection uses `(artist as any).images?.[0]?.url` and `(artist as any).external_urls?.spotify` as a workaround. Consider extending the interface or adding these fields.
- Issue: Existing ListeningHeatmap component has its own <section> wrapper and heading ("Your Sonic Rhythm") which creates a nested section hierarchy when embedded inside the dashboard. This works visually but is semantically non-standard. Acceptable for now; could refactor ListeningHeatmap to accept a `variant` prop in future.
